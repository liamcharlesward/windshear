import type { IconType } from "react-icons"

export interface TagProps {
  text?: string,
  icon?: IconType,
  shadow?: boolean,
  textColour?: string,
  backgroundColour?: string,
  fontSize?: number,
  fontWeight?: number,
};
